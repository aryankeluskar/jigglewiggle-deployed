/**
 * Re-export from shared module for backward compatibility.
 */
export {
  drawSkeleton,
  loadPose,
  POSE_CONNECTIONS,
} from "../shared/pose";

export type {
  NormalizedLandmark,
  PoseResults,
} from "../shared/pose";
